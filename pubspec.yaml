name: hand_sign_cricket
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  url_launcher:
  google_fonts:
  firebase_core:
  firebase_auth:
  shared_preferences:
  firebase_database:
  flutter_svg:


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/animation/
    # - assets/sounds/
    - assets/gestures/
    - assets/icons/

